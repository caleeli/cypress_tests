export class ModelerElementDestinationRedirect {
    /**
     * Configures the element destination for tasks.
     * @param nameElement - The name of the element.
     * @param optionConfig - The configuration options.
     * @param iframeOption - The iframe option (default is 'a').
     */
    configureElementDestinationInTask(nameElement, optionConfig, iframeOption = 'a') {
        // Method to configure the element destination in a task
    }

    /**
     * Selects an option in the element destination field in the task control.
     * @param optionConfig - Configuration options, including option, dashboardName, and url.
     * @param iframeOption - The iframe option ('a' for iframe A, 'b' for iframe B).
     */
    selectOptionInTask(optionConfig, iframeOption = 'a') {
        // Method to select an option in the element destination for a task
    }

    /**
     * Selects a dashboard in the modeler.
     * @param dashboardName - The name of the dashboard to select.
     * @param iframeOption - The iframe option ('a' for iframe A, 'b' for iframe B).
     */
    selectDashboardInModeler(dashboardName, iframeOption = 'a') {
        // Method to select a dashboard in the modeler
    }

    /**
     * Selects an external URL in the modeler.
     * @param url - The URL to select.
     * @param iframeOption - The iframe option ('a' for iframe A, 'b' for iframe B).
     */
    selectExternalURLInModeler(url, iframeOption = 'a') {
        // Method to select an external URL in the modeler
    }

    /**
     * Configures the element destination for end events.
     * @param nameElement - The name of the element.
     * @param optionConfig - The configuration options.
     * @param iframeOption - The iframe option (default is 'a').
     */
    configureElementDestinationInEndEvent(nameElement, optionConfig, iframeOption = 'a') {
        // Method to configure the element destination in an end event
    }

    /**
     * Selects an option in the element destination field in the end event control.
     * @param optionConfig - Configuration options, including option, dashboardName, url, and processName.
     * @param iframeOption - The iframe option ('a' for iframe A, 'b' for iframe B).
     */
    selectOptionInEndEvent(optionConfig, iframeOption = 'a') {
        // Method to select an option in the element destination for an end event
    }

    /**
     * Selects another process in the modeler.
     * @param processName - The name of the process to select.
     * @param iframeOption - The iframe option ('a' for iframe A, 'b' for iframe B).
     */
    selectAnotherProcessInModeler(processName, iframeOption = 'a') {
        // Method to select another process in the modeler
    }

    /**
     * Selects an element destination.
     * @param option - The option to select (e.g., 'Task Source', 'Task List').
     * @param iframeOption - The iframe option ('a' for iframe A, 'b' for iframe B).
     */
    selectElementDestination(option, iframeOption = 'a') {
        // Method to select an element destination
    }

    /**
     * Selects an option from the element destination dropdown.
     * @param option - The option to select.
     * @param iframe - The iframe in which to perform the action.
     */
    selectOption(option, iframe) {
        // Method to select an option from the element destination dropdown
    }

    /**
     * Clicks on the element destination dropdown.
     * @param iframeOption - The iframe option ('a' for iframe A, 'b' for iframe B).
     */
    clickOnElementDestination(iframeOption = 'a') {
        // Method to click on the element destination dropdown
    }

    /**
     * Validates the options in the element destination list.
     * @param expectedContentList - The expected list of options.
     * @param iframeOption - The iframe option ('a' for iframe A, 'b' for iframe B).
     */
    validateOptionsInElementDestinationList(expectedContentList, iframeOption = 'a') {
        // Method to validate options in the element destination list
    }
}
